Make sure the application or shortcut is in the subfolder called "applications"

Make sure there is a subfolder called "processing"

You have to enter the file extension along with the file name.

You can test it using the file "test.bat" which is in the applications folder, where you should put your applications,
enter the file name "test" followed by the extension which is ".bat"
so in the input you should have "test.bat"

if you are unsure what the file extension for a certain file is then in file explorer, click "view"
at the top of the window then enable "file name extensions" by clicking the check box.

if the file doesnt exist Admin App V3 will tell you and ask you to re-enter the file name.